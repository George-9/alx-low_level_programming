#include <stdio.h>

/**
* main - the program entry point
*
* Return: 0
*/
int main(void)
{
	char *msg;

	msg = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", msg);

	return (0);
}
