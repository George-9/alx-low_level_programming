#include <stdio.h>
#include <time.h>
#include <string.h>
#include <stdlib.h>

/**
* main - Entry point
*
*
*Return: Always 0 (Success)
*/
int main(void)
{
	int n, l;
	char *a;

	srand(time(0));
	n = RAND_MAX / 2;
	
	a = itoa(char [10], a, 10);

	if (n == 
}
