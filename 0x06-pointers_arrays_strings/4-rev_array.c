#include "main.h"

/**
* reverse_array - reverses a given  array of integers
*
* @a: array to reverse
* @n: num of items in array
*
* Return: void
*/
void reverse_array(int *a, int n)
{
int len;

	for (len = 0; len < (*(&i + 1) - a); len++)
	{

	}
}

