#include "main.h"

/**
* _isupper - Returns a num 1 or 0 according to the checked character
*
* @c: only parameter
*
* Return: int
*
*/
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	return (0);
}
