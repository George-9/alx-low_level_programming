#include "main.h"

/**
* _isdigit - Returns a num 1 if c is a digit else 0
*
* @d: only parameter
*
* Return: int
*
*/
int _isDigit(int d)
{
	for (d = 0; d < 9; d++)
	{
		if (isdigit(checker))
		{
			return (1);
		}
		return (0);
	}
}
