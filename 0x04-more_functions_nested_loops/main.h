#include <string.h>
#include <ctype.h>

/**
* _isupper - Returns a num 1 if c is in uppercase else 0
*
* @c: only parameter
*
* Return: int
*
*/
int _isupper(int c);

/**
* _isdigit - Returns a num 1 if c is a digit else 0
*
* @c: only parameter
*
* Return: int
*
*/
int _isdigit(int c);

/**
* mul - Returns a num 1 if c is a digit else 0
*
* @a: num parameter
* @b: num param2
*
* Return: int
*
*/
int mul(int a, int b);
