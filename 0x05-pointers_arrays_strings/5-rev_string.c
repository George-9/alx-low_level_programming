#include "main.h"

/**
* rev_string - returns the param as a reversed string
*
* @s: param
* Return: String
*/
void rev_string(char *s)
{
int i, len;
char *d;

	len = strlen(s);

	for (i = len; i > -1; i--)
	strcopy(d, s[i])
}
